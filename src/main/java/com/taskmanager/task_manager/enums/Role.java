package com.taskmanager.task_manager.enums;

/**
 * Application roles used for authorization.
 */
public enum Role {
    /** Administrator with elevated permissions. */
    ADMIN,
    /** Standard user with regular permissions. */
    USER
}
